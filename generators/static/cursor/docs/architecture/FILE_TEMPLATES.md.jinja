# File templates — [[project_name]]

## Domain entity `<entity>` (snake_case folder)

### model/

| File | Purpose |
|------|---------|
| `<entity>.dart` | Freezed entity |
| `<entity>_failure.dart` | Freezed failure union |
| `i_<entity>_repository.dart` | Repository interface (`Either<Failure, T>`) |
| `value_objects.dart` | Value objects for fields |
| `value_validators.dart` | Field validators |

### infrastructure/

| File | Purpose |
|------|---------|
| `<entity>_dto.dart` | JSON DTO |
| `i_<entity>_service.dart` | Service interface |
| `<entity>_remote_service.dart` | Retrofit REST client |
| `mock_<entity>_service.dart` | Mock service (reads `assets/mock/<entity>.json`) |
| `<entity>_service_module.dart` | DI binding mock vs remote |
| `<entity>_mapper.dart` | DTO ↔ entity |
| `<entity>_repository.dart` | `@Injectable` repository (injects `I*Service`) |

### Mock assets

| File | Purpose |
|------|---------|
| `assets/mock/<entity>.json` | Seed data (`{ "items": [...] }`) |

{% if login %}
### Auth infrastructure

| File | Purpose |
|------|---------|
| `domain/auth/infrastructure/auth_facade.dart` | Remote auth skeleton |
| `domain/auth/infrastructure/mock_auth_facade.dart` | Mock auth |
| `domain/auth/infrastructure/auth_facade_module.dart` | DI for `IAuthFacade` |
| `assets/mock/auth_user.json` | Mock user seed |
{% endif %}

## Feature `<feature>` with BLoC

### application/

| File | Purpose |
|------|---------|
| `<name>_bloc.dart` | BLoC |
| `<name>_event.dart` | Freezed events |
| `<name>_state.dart` | Freezed states |

### presentation/

| File | Purpose |
|------|---------|
| `<name>_page.dart` | Route page |
| `<name>_component.dart` | Embeddable component |

## Reusable component (`lib/features/components/<name>/`)

Same `application/` + `presentation/` structure as a feature.

## Enum

`lib/domain/enums/<enum_snake>.dart` — PascalCase enum class.

## Integration (after layers)

- `lib/apis/common/data_source_config.dart` — per-entity mock/remote
- `lib/router.dart` — GoRouter routes
- `lib/injection.dart` — GetIt / injectable init
- `lib/core/errors/error_localizer.dart` — user-facing failure messages

See `docs/architecture/MOCK_AND_REMOTE_DATA.md` for mock JSON and switching to remote.
